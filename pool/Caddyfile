{
    # Desativa o HTTP automático, usamos apenas HTTPS
    auto_https disable_redirects
    local_certs
}

localhost:8443 {
    tls internal

    # Proxy para o backend da pool
    reverse_proxy http://localhost:8080

    # Log de requisições para debug
    log {
        output stdout
        format console
    }
}
